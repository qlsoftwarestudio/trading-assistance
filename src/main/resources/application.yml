spring:
  application:
    name: trading-assistant
  
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/tradingassistant}
    username: ${SPRING_DATASOURCE_USERNAME:trading}
    password: ${SPRING_DATASOURCE_PASSWORD:trading123}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:create-drop}
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

# Binance Configuration
binance:
  api:
    key: ${BINANCE_API_KEY:}
    secret: ${BINANCE_SECRET_KEY:}
    base-url: ${BINANCE_BASE_URL:https://testnet.binancefuture.com}
    websocket-url: ${BINANCE_WS_URL:wss://testnet.binancefuture.com/ws}
  testnet: ${BINANCE_TESTNET:true}

# Trading Strategy Configuration
trading:
  strategy:
    symbol: HYPEUSDT
    timeframe: 15m
    rsi-length: 5
    rsi-oversold: 30
    rsi-overbought: 70
    lookback-bars: 12
    killzone-threshold: 1.0
    min-momentum: 0.8
    stop-loss-pct: 2.0
    take-profit-pct: 4.0
    position-size-pct: 20.0
    leverage: 5
    enabled: true
    # Risk Management - ATR-based dynamic stop (recommended for volatile assets)
    # When enabled: SL = entry ± (ATR * multiplier), TP = 2x risk (reward/risk = 2:1)
    use-atr-stop: false
    atr-period: 14
    atr-multiplier: 2.0

  # Market Context / Multi-timeframe Analysis
  context:
    enabled: true
    require-confluence: false
    require-volume: true
    min-volume-ratio: 1.0

  # Historical Performance & Auto-Adjustment
  performance:
    auto-adjust: false
    min-samples: 20
    adjustment-delta-pct: 5.0

# Telegram Configuration
telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN:}
    chat-id: ${TELEGRAM_CHAT_ID:}
    enabled: ${TELEGRAM_ENABLED:false}

# Logging
logging:
  level:
    com.trading.assistant: DEBUG
    org.springframework.web: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
